# PROJECT STATUS

## 현재 상태

- 헤더 StoryWell Ver2.0 변경은 GitHub main에 커밋·푸시 완료(d7852b8). 사용자 요청 대상은 Vercel이며, 배포 대상 확인 대기. 아래 버전 12는 현재 운영 중인 Sites 버전으로 헤더 변경이 아직 게시되지 않았음

- 2026-09-13 18:28 KST, StoryWell 버전 12 게시 성공 확인
- 사이트: https://storywell-webnovel-studio.homong-lee.chatgpt.site
- 게시된 코드: 750f091b5349d1180367443ddb3c73603eda783d
- 해당 코드는 GitHub main과 Sites 게시 저장소에 저장 완료
- 기존 주소, 소유자 전용 접근 범위, 데이터베이스, API 키 유지

## 완료사항

- 사용자매뉴얼 버튼과 단계별 매뉴얼 게시
- GPT 모델 선택 메뉴 게시 및 사용자별 브라우저에 선택값 저장
- 선택 모델을 AI 설계·집필·편집·연속성 검사 요청에 전달
- 서버에서 지원 모델만 허용하도록 검증
- 모델 선택·내보내기 버튼을 Radix 메뉴의 asChild 방식으로 수정
- Windows 노트북에 잠금 목록 기준 패키지 설치 완료
- TypeScript 검사, 최신 코드 빌드, 배포 파일 검증 통과
- Sites 배포 상태 succeeded 및 기존 주소 확인

## 주요 결정사항

- 기존 OpenAI API 키를 계속 사용하며 키 값은 로컬로 복사하지 않음
- 선택 가능한 모델: GPT-5.6 Terra, Sol, Luna, GPT-6 Astra 및 서버 기본 모델
- 모델 선택은 사용자별 브라우저 설정으로 유지
- 초기 기능 작업에서는 실제 AI 생성 검증이 빠졌음. 아래 AI 장애 수정에서 운영 사이트의 실제 설계·집필·편집·연속성 검사·소재 생성을 검증 완료. 모든 모델의 품질 비교는 수행하지 않음

## 이 노트북에서 이어가기

- 프로젝트: C:\Users\tiger\OneDrive\문서\GitHub\storywell
- 실행 환경: Node.js 24.19.0, pnpm 11.25.0, Sites portable
- Windows 의존성 설치는 pnpm install --frozen-lockfile 사용
- 기존 install:ci는 Linux 전용이므로 Windows에서 사용하지 않음
- 로컬에는 API 키가 없고, 게시된 사이트의 비밀 환경변수에 키가 등록되어 있음
- 로컬 확인: pnpm dev / 타입 검사: pnpm exec tsc --noEmit --incremental false
- GitHub origin: https://github.com/homonglee/storywell.git
- Sites 게시 저장소는 GitHub origin과 별개이며 기존 project_id를 계속 사용

## 다음 작업

- 어제 남겨둔 모델 선택 기능 게시와 배포 완료 확인은 모두 완료
- 추가 기능과 반응형 수정은 GitHub에 저장하고 기존 사이트에 게시 완료. 게시 후 상태 기록은 문서 변경으로 별도 저장

## 2026-09-13 추가 기능 — 구현·검증·게시 완료

- 화면 위쪽에 작품 삭제·다시 불러오기 버튼 추가
- AI 작업 중 작업 취소 버튼 표시 및 요청 중단 처리
- 삭제 확인창에서 작품 제목과 원고·AI 기록의 삭제 범위 안내
- 작품과 해당 소유자의 AI 생성 기록을 하나의 트랜잭션으로 삭제
- 삭제 오류 시 전체 변경을 되돌리고, 다른 사용자의 자료는 변경하지 않음
- 취소 뒤 늦게 도착한 AI 결과를 화면과 원고에 적용하지 않음
- 저장되지 않은 변경사항이나 수정안이 있으면 다시 불러오기 전 확인
- 다시 불러오기에 실패하면 현재 원고 유지
- 매뉴얼에 삭제·취소·다시 불러오기 사용법 추가
- 검증: scripts/test-workspace-controls.cjs의 9개 테스트 통과, TypeScript 검사·빌드 성공, 로컬 미리보기 HTTP 200
- 실제 기존 작품은 삭제하지 않았으며 OpenAI 유료 생성 요청도 실행하지 않음
- GitHub main 업로드는 자동 승인 검토에서 명시적 업로드 승인 부족으로 거절됨. 이후 사용자가 “항상 승인함”으로 이번 기능과 반응형 수정의 업로드·게시를 승인하여 완료
- 해당 작업은 버전 9에 반영되었으며, 현재 배포 버전은 문서 맨 위의 현재 상태 참조


## 2026-09-13 반응형 점검 — 수정·검증·게시 완료

- 재현: 390px 휴대폰 화면에서 헤더가 573px까지 넘치며 로고와 매뉴얼 버튼이 겹침
- 수정: 800px 이하에서는 로고와 헤더 버튼을 두 줄로 분리하고, 520px 이하에서는 버튼 4개를 2열로 배치
- 로고·작품 목록 버튼이 찌그러지지 않도록 고정하고, 긴 모델명은 버튼 내부에서 말줄임 처리
- 휴대폰 헤더 버튼의 터치 영역을 44px 높이로 확보
- 작은 화면의 복선 등록·AI 연속성 검사 버튼도 공간이 부족하면 다음 줄로 배치
- 브라우저 뷰포트 320·390·768·약 800·1024·약 1100·1440·1920px에서 헤더 겹침과 화면 밖 버튼 없음 확인
- 320px 휴대폰 및 1440px 데스크톱에서 전체 설계·캐릭터·회차·집필·복선·소재 우물·AI 버전의 페이지 가로 넘침 0 확인
- 작은 휴대폰의 매뉴얼 창·내보내기 메뉴가 화면 안에 표시되고 작품 목록 열기·닫기가 정상 동작함을 확인
- 로컬 저장소가 비어 있어 기존 drizzle 테이블을 로컬에만 초기화한 뒤 다시 불러오기 성공 확인
- 최종 브라우저 오류 로그 없음, TypeScript 검사 및 최종 빌드 성공
- 검증은 브라우저 화면 크기 시뮬레이션으로 수행했으며 실제 휴대폰 기기 테스트는 하지 않음
- 로컬 미리보기: http://127.0.0.1:8787
- 2026-09-13 사용자 “항상 승인함” 응답에 따라 삭제·취소·다시 불러오기 및 반응형 수정의 GitHub 업로드와 기존 사이트 게시 완료

- 추가 시각 점검: 탭 메뉴의 불필요한 세로 스크롤바 제거, 선택 표시 복구 및 모바일 가로 스크롤 색상 정돈. 재빌드 후 데스크톱·320px 화면 확인 완료.

## 최근 게시 결과와 작업 선호

- Sites 배포 상태 succeeded, 기존 소유자 전용 접근과 환경 설정 유지
- 사용자 요청: 이 프로젝트에서 승인한 수정·업로드·기존 사이트 게시 작업은 반복 확인 없이 진행. 앱 자체의 승인 정책은 유지
- 다음 노트북 또는 데스크톱에서 최신 main을 받은 뒤 이 문서 기준으로 작업을 이어갈 수 있음

## 2026-09-13 회차별 목표 글자 수 — 구현·검증·게시 완료

- 회차 목록과 집필 화면에서 각 장의 목표 글자 수를 개별 입력
- 공백 포함 1~20,000자, 기존 작품 및 빈 입력은 기본 5,000자
- 저장 또는 원고 저장 시 목표와 원고를 함께 저장하고 다시 불러와도 유지
- 현재 글자 수, 목표 대비 달성률, 남은 분량 표시
- AI 회차 집필에 해당 목표를 전달하고 긴 목표에 맞춰 출력 한도를 조정
- 전체 설계 재생성 및 설계 버전 복원 시 회차 번호를 기준으로 기존 목표 보존
- 기존 JSON 작품 저장 형식을 확장하여 데이터베이스 구조 변경 없음
- 잘못된 값은 입력창과 저장·AI 요청 단계에서 검증
- 검증: 실제 저장 API 및 임시 DB, AI 요청 대역을 사용하는 14개 테스트 통과
- TypeScript 검사, 빌드, 변경 파일 점검 통과
- 로컬 브라우저에서 1화 3,200자·2화 6,800자 각각 저장 후 유지, 빈 입력 기본값, 잘못된 값 거절 확인
- 320·390·768·1024·1440px에서 회차 목록과 집필 입력창의 가로 넘침 없음, 브라우저 오류·경고 없음
- UI 검증에 만든 로컬 테스트 작품만 정리했으며 기존 운영 작품 및 유료 AI 생성은 사용하지 않음

- 2026-09-13 16:56 KST 기존 StoryWell 주소에 버전 8 게시 성공(succeeded), 소유자 전용 접근·기존 API 연결 유지

## 2026-09-13 새 작품 목표 글자 수 및 인물 편집 — 구현·검증·게시 완료

- 새 작품 설계에 회차당 목표 글자 수 입력 추가. 공백 포함 1~20,000자, 기본 5,000자
- 생성한 모든 회차에 입력한 목표를 적용하며 기존 회차별 개별 수정 기능과 연결
- 캐릭터 카드에 편집 버튼 추가. 이름·역할·인물 유형·욕망·두려움·숨은 비밀·목소리와 말투·현재 상태·표시 색상 수정 가능
- 인물 추가도 같은 편집창을 사용하고 저장 후 카드에 반영. 빈 이름은 저장 차단
- 인물 저장은 서버에 즉시 저장하며, 취소하면 편집 전 카드 유지. 저장 오류 시 편집 내용 유지
- 인물 삭제 확인창에 대상 이름과 삭제 범위 표시. 고유 ID로 해당 인물만 삭제하고 기존 원고 및 다른 인물 유지
- 새 작품 및 인물 편집창은 작은 화면에서 한 열로 배치하고 긴 창은 내부 스크롤
- 매뉴얼에 새 작품 목표 글자 수와 인물 편집·저장·취소·삭제 방법 추가
- 검증: 회귀 테스트 18개, TypeScript 검사, 배포 빌드, git diff --check 통과
- 로컬 실제 UI에서 3,400자·12회차 생성 후 모든 회차 저장값 확인
- 이름과 모든 인물 항목 및 색상 수정 → 저장 → 다시 불러오기 유지, 편집 취소, 삭제 확인 취소, 새 인물 추가 확인
- 320·390·768·1440px 화면에서 새 입력창과 인물 편집창 반응형 확인. 가로 넘침 및 브라우저 오류 없음. 실제 휴대폰 기기 대신 화면 크기 시뮬레이션 사용
- 검증용 로컬 작품은 정리 완료. 운영 작품은 변경하지 않았으며 유료 AI 생성은 호출하지 않음
- 2026-09-13 17:27 KST, 버전 9 게시 succeeded 확인. 기존 주소와 소유자 전용 접근, 환경 설정 유지. 게시 소스는 GitHub main 및 Sites 전용 저장소에 저장 완료


## 2026-09-13 AI 핵심 기능 장애 수정 — 구현·검증·게시 완료

- 원인 1: 새 작품 생성이 AI를 호출하지 않고 buildStory의 고정 캐릭터·회차·원고·기억 예시를 복사함
- 원인 2: 모든 AI 요청을 서버가 55초에 강제 중단. 운영 오류 기록 4건이 55,000~55,001ms에 HTTP 500으로 끝남. 짧은 실제 AI 문장 요청은 3.3초에 성공하여 키와 기본 모델 연결은 확인
- 원인 3: 건강도 96점, 이번 화 검사, 설정 잠금, 소재 제안·적용, 복선 등록 일부가 고정 문구 또는 알림만 처리
- 새 작품은 빈 설계 대기 상태를 먼저 저장한 뒤 실제 AI 인물·세계관 설계와 최대 20회차씩 전체 구성을 생성. 샘플 함수는 샘플 보기에만 사용
- AI Responses 스트리밍을 읽고 브라우저에 진행 단계·생성 본문·연결 유지 신호 전달. 기존 55초 제한을 긴 작업에 맞는 12분 상한으로 변경
- 회차 수·순서·제목 및 사건 중복 검증, 잘린 출력·거절·연결 중단은 완료로 저장하지 않음
- 실패하거나 취소하면 기존 작품 유지. 새 작품의 제목·시놉시스는 보관함에서 다시 AI 설계 가능
- 예전 고정 예시가 그대로인 항목은 AI 설계 자료에서 제외하되 저장된 원고와 작가 수정 항목 보존
- AI 설계 병합 시 회차별 목표·기존 원고 상태·인물 ID 및 색상·잠긴 세계관 규칙 보존
- 현재 회차까지의 원고를 문맥에 포함하고 미래 회차를 참조하지 않도록 수정. 연속성 검사에서 기존 기억과 잠긴 사실 보존
- 소재 제안은 실제 AI 생성으로 교체. 소재 적용은 현재 회차 사건에 후보를 저장. 설정 잠금은 저장 및 AI 문맥에 연결
- 복선 등록·편집 입력창과 저장 기능 추가. 건강도 고정 점수 대신 실제 검사 여부와 확인 항목 수 표시
- 회귀 테스트 28개 통과. TypeScript 검사·빌드 성공. AI 비연결 시 가짜 작품이 생기지 않으며 입력값 유지, 모바일 복선 편집 확인
- 공식 Responses 스트리밍 및 Structured Outputs 문서 확인. 실제 운영 AI 전체 흐름 검증은 아래 게시 결과에 별도 기록


## 2026-09-13 AI 장애 수정 — 운영 검증과 최종 게시

- 기존 고정 샘플 복사와 55초 강제 중단이 실제 장애 원인이었음. 키·모델 오류로 단정하지 않고 짧은 실제 요청과 운영 오류 시간을 대조하여 확인
- 새 작품 설계 → 인물·세계관 생성 → 20회차 단위 구성 → 저장까지 실제 AI를 연결. 예전 샘플 작품은 AI로 전체 설계를 실행해 다시 설계할 수 있으며 기존 원고와 작가가 수정한 설정은 보존
- 운영 경로에서 NDJSON과 SSE가 모두 모여서 전달되는 현상 확인. 스트림 형식만 바꾼 버전 11로는 진행 표시가 해결되지 않았으며, 버전 12에서 2초 간격의 진행 조회를 추가하여 해결
- 진행 조회는 작품 소유자 범위에서만 허용. 최근 미리보기 1,200자와 상태를 story_ai_progress 테이블에 저장하며 하루 지난 자료는 조회하지 않고 다음 요청에서 정리
- drizzle/0002_ai_progress.sql 마이그레이션이 배포된 DB에 적용된 것을 Sites 데이터베이스 목록으로 확인
- 실제 운영 검증 작품: AI 복구 검증 · 심해의 마지막 우편선. 가상 시놉시스로 별도 생성하여 확인용 예시로 남김. 기존 질문의 온도 36.5도 및 검증용: 유리비가 내리던 밤은 변경·삭제하지 않음
- 사용자가 선택해 둔 GPT-5.6 Sol로 21회차 설계 완료. 소요 190,085ms(약 3분 10초), 인물 6명(나린·해수·모스·여진·윤서·도겸), 복선 9개, 21회차 각 목표 5,000자 저장 확인
- 원고 실제 생성 6,680자, 연속성 검사 후 기억 카드 14개 저장. 목표 글자 수는 생성 지침이며 정확히 일치하는 길이를 보장하지 않음
- 버전 12에서 원고 다듬기 중 12초 시점에 실제 생성 문장과 진행 단계 표시, 39초 시점에 최근 1,200자 표시 확인. 390px 휴대폰 화면에서 가로 넘침 없음
- 원고 수정안 비교 → 적용 → 6,803자 원고 저장 완료. 소재 제안으로 새 소재 3개 생성 후 하나를 현재 회차 사건의 소재 후보로 적용
- 실제 진행 중 작업 취소 후 6,803자 원고 유지, 미리보기 종료 및 작업 버튼 복구. 완료된 AI 이력에는 전체 설계·회차 원고·연속성 검사·원고 다듬기·소재 제안 5건 확인
- 세계관 규칙 잠금 및 기존 복선의 상태 변경 저장 확인. 새로고침 후 수정한 원고·소재 후보·잠금·복선 상태 유지 확인
- 최종 회귀 테스트 30개, TypeScript 검사, 배포 빌드 및 배포 파일 검증 통과. 21회차 실제 설계는 확인했으며 80·200회차 전체 생성 및 모든 모델 조합은 실제 호출하지 않음
- 버전 12: appgprj_6aa50dc559308191a71aa3139d16e33a~appgver_0a66c1f344cc81918d95a43a02d5ee25
- 배포: appgdep_6aa66c8842c08191afa285bf687b8282, succeeded. 같은 사이트 주소·소유자 전용 접근·기존 API 키 유지
- 새 코드를 받으려면 사이트를 새로고침. 기존에 고정 샘플로 만들어진 작품은 해당 작품에서 AI로 전체 설계를 실행. 긴 설계는 진행 단계가 표시되는 동안 기다리거나 작업 취소 가능

## 2026-09-13 StoryWell Ver2.0 헤더 및 Vercel 배포 요청

- 헤더 표시를 StoryWell Ver2.0으로 변경. 기능과 저장 자료는 변경하지 않음
- TypeScript 검사와 기존 Sites 배포 빌드 성공, Git diff 검사 통과
- 소스 커밋 d7852b8, GitHub main 푸시 완료
- Vercel 팀 조회 결과 teams가 비어 있고, .vercel/project.json 및 Vercel CLI 로그인 설정을 찾지 못함
- Vercel 배포 도구 호출은 입력 검증 단계에서 중단되었으며 배포가 만들어지지 않음
- 이 앱은 현재 Cloudflare Workers·D1·Sites 인증에 연결되어 있어 Vercel로 옮기려면 서버 실행 환경·데이터 저장·인증 연결도 전환해야 함
- 배포 대상 Vercel 프로젝트 주소/팀 또는 기존 Sites 게시 의도 확인을 사용자에게 요청함. Vercel 배포 성공으로 보고하지 않았으며 Sites에도 대신 게시하지 않음
